<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="ButtonStyle" parent="Widget.AppCompat.Button">
        <item name="android:background">@drawable/custom_button_background</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginHorizontal">50dp</item>

        <item name="android:minHeight">48dp</item>
        <item name="android:minWidth">120dp</item>
        <item name="android:layout_marginTop">8dp</item>
        <item name="android:layout_marginBottom">8dp</item>
    </style>

    <style name="ButtonStyle.Gray" parent="ButtonStyle">
        <item name="android:background">@drawable/custom_button_background_gray</item>
    </style>

    <style name="ButtonStyle.Blue" parent="ButtonStyle">
        <item name="android:background">@drawable/custom_button_background_blue</item>
    </style>

</resources>
